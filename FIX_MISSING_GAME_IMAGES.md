# 🖼️ แก้ไขปัญหารูปเกมหาย

## ❌ ปัญหา
- รูปเกมที่เพิ่มเข้าไปหายหมด
- รูปเกมไม่แสดงใน production

## 🔍 สาเหตุ
1. **URL ผิด:** รูปเกมถูกบันทึกเป็น `http://localhost:5000/uploads/...` ซึ่งไม่ทำงานใน production
2. **ไฟล์หาย:** เมื่อรัน seed.sql ใหม่ เกมที่เพิ่มเองอาจหายไป
3. **Environment Variable:** ไม่ได้ตั้งค่า `BASE_URL` ใน Render

---

## ✅ วิธีแก้ไข

### **Step 1: อัพเดท Environment Variable ใน Render**

1. ไปที่ **Render Dashboard** → **game-key-marketplace** → **Environment**
2. เพิ่ม Environment Variable:
   ```
   BASE_URL=https://game-key-marketplace.onrender.com
   ```
3. **Redeploy** service

---

### **Step 2: ตรวจสอบเกมที่เพิ่มเอง**

1. ไปที่ **Supabase Dashboard** → **SQL Editor**
2. รันคำสั่งนี้เพื่อดูเกมทั้งหมด:
   ```sql
   SELECT game_id, name, platform, image_url 
   FROM games 
   ORDER BY game_id DESC;
   ```
3. ตรวจสอบว่าเกมที่เพิ่มเองยังอยู่หรือไม่

---

### **Step 3: อัพเดท URL ของรูปเกมเก่า**

ถ้ารูปเกมยังอยู่ในฐานข้อมูล แต่ URL เป็น `localhost` ให้อัพเดท:

```sql
-- อัพเดท URL ของรูปเกมทั้งหมดที่ใช้ localhost
UPDATE games 
SET image_url = REPLACE(image_url, 'http://localhost:5000', 'https://game-key-marketplace.onrender.com')
WHERE image_url LIKE 'http://localhost:5000%';
```

---

### **Step 4: ตรวจสอบไฟล์ใน uploads folder**

⚠️ **สำคัญ:** ไฟล์ใน `backend/uploads/` จะไม่ถูก deploy ไปที่ Render อัตโนมัติ

**วิธีแก้ไข:**
1. **Option A: ใช้ External Storage (แนะนำ)**
   - ใช้ Cloudinary, AWS S3, หรือ Supabase Storage
   - อัพโหลดรูปไปที่ external storage
   - บันทึก URL ลงฐานข้อมูล

2. **Option B: ใช้ Render Disk (จำกัด)**
   - Render มี persistent disk แต่มีข้อจำกัด
   - ไฟล์อาจหายเมื่อ redeploy

3. **Option C: ใช้ External Image URLs**
   - ใช้ URL จาก external source (เช่น PlayStation API, Nintendo API)
   - ไม่ต้องเก็บไฟล์ใน server

---

## 🔄 วิธีกู้คืนรูปเกม

### **ถ้าเกมยังอยู่ในฐานข้อมูล:**

1. **ดูเกมทั้งหมด:**
   ```sql
   SELECT game_id, name, platform, image_url 
   FROM games 
   WHERE image_url LIKE '%localhost%' OR image_url IS NULL OR image_url = '';
   ```

2. **อัพเดท URL:**
   ```sql
   -- อัพเดท URL ที่เป็น localhost
   UPDATE games 
   SET image_url = REPLACE(image_url, 'http://localhost:5000', 'https://game-key-marketplace.onrender.com')
   WHERE image_url LIKE 'http://localhost:5000%';
   ```

3. **เพิ่ม placeholder สำหรับเกมที่ไม่มีรูป:**
   ```sql
   UPDATE games 
   SET image_url = 'https://via.placeholder.com/460x215/EF4444/ffffff?text=' || REPLACE(name, ' ', '+')
   WHERE image_url IS NULL OR image_url = '';
   ```

---

### **ถ้าเกมหายไป:**

1. **ตรวจสอบว่าเกมถูกลบหรือไม่:**
   ```sql
   -- ดูเกมทั้งหมด
   SELECT COUNT(*) FROM games;
   ```

2. **ถ้าเกมหาย ต้องเพิ่มใหม่:**
   - ไปที่ Admin Panel
   - เพิ่มเกมใหม่
   - อัพโหลดรูปใหม่

---

## 📋 Checklist

- [ ] เพิ่ม `BASE_URL` ใน Render Environment Variables
- [ ] Redeploy Backend service
- [ ] ตรวจสอบเกมในฐานข้อมูล
- [ ] อัพเดท URL ของรูปเกมเก่า (ถ้าจำเป็น)
- [ ] ทดสอบว่าเห็นรูปเกมใน production

---

## 🎯 สรุป

**วิธีแก้ไข:**
1. เพิ่ม `BASE_URL=https://game-key-marketplace.onrender.com` ใน Render
2. Redeploy Backend
3. อัพเดท URL ของรูปเกมเก่าในฐานข้อมูล (ถ้าจำเป็น)
4. ใช้ External Storage สำหรับรูปเกมในอนาคต (แนะนำ)

**⚠️ หมายเหตุ:**
- ไฟล์ใน `backend/uploads/` จะไม่ถูก deploy ไปที่ Render
- ควรใช้ External Storage (Cloudinary, AWS S3) สำหรับรูปเกม
- หรือใช้ External Image URLs จาก API

---

**✅ แก้ไขแล้ว!**

