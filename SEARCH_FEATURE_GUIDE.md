# 📖 คู่มือการใช้งานระบบ Search (ค้นหาเกม)

## 🎯 ฟีเจอร์ที่เพิ่มเข้ามา

ระบบค้นหาเกมในหน้า Home ที่สามารถค้นหาได้ทั้ง:
- ชื่อเกม
- แพลตฟอร์ม (Steam, Origin, etc.)
- คำอธิบายเกม

---

## 🖥️ วิธีการใช้งาน

### 1. **บน Desktop/Laptop**
- ดูที่ **Navbar ด้านบนขวา** จะมีช่องค้นหา (Search Bar)
- พิมพ์คำที่ต้องการค้นหา เช่น "Valorant", "Steam", "FPS" ฯลฯ
- ระบบจะค้นหาแบบ Real-time (ค้นหาทันทีที่พิมพ์)
- กดปุ่ม **X** เพื่อล้างการค้นหา

### 2. **บน Mobile/Tablet**
- ดูที่ **ส่วนบนของหน้า** (ใต้ Hero Banner) จะมี Search Bar สำหรับมือถือ
- พิมพ์คำที่ต้องการค้นหา
- ระบบจะแสดงจำนวนผลลัพธ์ที่พบ
- กดปุ่ม **ล้าง** เพื่อล้างการค้นหา

---

## ✨ ฟีเจอร์ที่ทำงาน

### ✅ **Real-time Search**
- ค้นหาทันทีที่พิมพ์ (ไม่ต้องกด Enter)
- ไม่ต้องกดปุ่มค้นหา

### ✅ **Smart Filtering**
- ค้นหาได้ทั้งชื่อเกม, แพลตฟอร์ม, และคำอธิบาย
- ไม่สนใจตัวพิมพ์ใหญ่-เล็ก (Case-insensitive)
- ค้นหาทุกส่วนของคำ (Partial match)

### ✅ **Visual Feedback**
- แสดงจำนวนผลลัพธ์ที่พบ
- แสดงข้อความ "ผลการค้นหา" แทน "สินค้าแนะนำ"
- แสดงข้อความเมื่อไม่พบผลลัพธ์

### ✅ **Clear Search**
- ปุ่ม X เพื่อล้างการค้นหา
- ปุ่ม "ล้างการค้นหา" ในส่วนผลลัพธ์

### ✅ **Responsive Design**
- Search Bar แยกสำหรับ Desktop และ Mobile
- ใช้งานได้ดีทั้งหน้าจอใหญ่และเล็ก

---

## 📱 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: ค้นหาชื่อเกม
```
พิมพ์: "Valorant"
ผลลัพธ์: แสดงเกม Valorant ทั้งหมด
```

### ตัวอย่างที่ 2: ค้นหาแพลตฟอร์ม
```
พิมพ์: "Steam"
ผลลัพธ์: แสดงเกมทั้งหมดที่อยู่ในแพลตฟอร์ม Steam
```

### ตัวอย่างที่ 3: ค้นหาคำอธิบาย
```
พิมพ์: "FPS"
ผลลัพธ์: แสดงเกมที่มีคำว่า FPS ในคำอธิบาย
```

### ตัวอย่างที่ 4: ไม่พบผลลัพธ์
```
พิมพ์: "XYZ123"
ผลลัพธ์: แสดงข้อความ "ไม่พบผลการค้นหา" พร้อมปุ่มล้าง
```

---

## 🎨 UI/UX Features

1. **Search Icon** - แสดงเมื่อยังไม่มีการค้นหา
2. **X Button** - แสดงเมื่อมีการพิมพ์ข้อความ (เพื่อล้าง)
3. **Result Count** - แสดงจำนวนเกมที่พบ
4. **Empty State** - แสดงข้อความและไอคอนเมื่อไม่พบผลลัพธ์
5. **Smooth Transitions** - การเปลี่ยนหน้าจอที่ลื่นไหล

---

## 🔧 Technical Details

### State Management
- `searchQuery`: เก็บคำที่ค้นหา
- `filteredGames`: เก็บผลลัพธ์ที่กรองแล้ว
- `games`: เก็บข้อมูลเกมทั้งหมด

### Search Logic
```javascript
// ค้นหาใน 3 ฟิลด์:
1. game.name (ชื่อเกม)
2. game.platform (แพลตฟอร์ม)
3. game.description (คำอธิบาย)
```

### Performance
- ใช้ `useEffect` เพื่อกรองข้อมูลแบบ Real-time
- ไม่ต้องเรียก API ใหม่ (กรองจากข้อมูลที่มีอยู่)

---

## 🚀 การทดสอบ

1. เปิดหน้า Home
2. พิมพ์คำค้นหาในช่อง Search
3. ดูผลลัพธ์ที่เปลี่ยนไปทันที
4. ลองพิมพ์คำที่ไม่มีในระบบ
5. กดปุ่ม X เพื่อล้างการค้นหา
6. ทดสอบทั้งบน Desktop และ Mobile

---

## 💡 Tips

- ค้นหาได้ทั้งภาษาไทยและอังกฤษ
- ไม่ต้องพิมพ์คำเต็ม (Partial match)
- ไม่ต้องกด Enter (Real-time search)
- ใช้ปุ่ม X เพื่อล้างการค้นหาได้เร็ว

---

## 🐛 Troubleshooting

**ปัญหา: ไม่พบผลลัพธ์**
- ตรวจสอบว่ามีเกมในระบบหรือไม่
- ลองค้นหาด้วยคำอื่น
- ตรวจสอบว่าพิมพ์ถูกต้อง

**ปัญหา: Search Bar ไม่แสดง**
- Desktop: ดูที่ Navbar ด้านบนขวา (ต้องหน้าจอใหญ่กว่า lg)
- Mobile: ดูที่ส่วนบนของหน้า (ใต้ Hero Banner)

---

**สร้างเมื่อ:** 2025
**เวอร์ชัน:** 1.0.0

