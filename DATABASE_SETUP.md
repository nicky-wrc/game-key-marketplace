# 🗄️ Database Setup Guide

## ปัญหาที่พบ
- Database ยังไม่มี tables ใหม่: `categories`, `reviews`, `wishlists`
- หน้าเว็บแสดง error "ไม่สามารถโหลดข้อมูลได้"
- เกมหายหมดจากหน้าแรก

## วิธีแก้ไข

### วิธีที่ 1: รันทั้งหมดพร้อมกัน (แนะนำ)
```bash
cd backend
npm run setup
```

### วิธีที่ 2: รันทีละขั้นตอน

#### ขั้นตอนที่ 1: รัน Migration (สร้าง Tables)
```bash
cd backend
npm run migrate
```

คำสั่งนี้จะ:
- สร้าง tables ที่ขาดหายไป (categories, reviews, wishlists, etc.)
- สร้าง indexes สำหรับ performance

#### ขั้นตอนที่ 2: รัน Alter Tables (เพิ่ม Columns)
```bash
cd backend
npm run alter
```

คำสั่งนี้จะ:
- เพิ่ม columns ที่ขาดหายไปใน existing tables
- อัพเดท schema ให้ตรงกับ code

#### ขั้นตอนที่ 3: รัน Seed Data (ใส่ข้อมูลตัวอย่าง)
```bash
cd backend
npm run seed
```

คำสั่งนี้จะ:
- เพิ่ม 8 หมวดหมู่
- เพิ่ม 60+ เกม
- เพิ่ม 550+ รหัสเกม
- เพิ่ม 12 Mystery Boxes
- เพิ่ม 12 Coupons
- เพิ่ม Users (admin, seller, testuser)

### ขั้นตอนที่ 3: ตรวจสอบผลลัพธ์
หลังจากรันเสร็จแล้ว:
1. ตรวจสอบ console ว่ามี tables และข้อมูลครบหรือไม่
2. รีเฟรชหน้าเว็บ
3. ตรวจสอบว่าเกมแสดงที่หน้าแรกแล้ว
4. ตรวจสอบว่า Profile page ทำงานได้แล้ว

## ข้อมูลสำหรับทดสอบ

### Login Credentials:
- **Admin**: `admin@nickykey.com` / `admin123`
- **Seller**: `seller@nickykey.com` / `admin123`
- **User**: `test@nickykey.com` / `admin123`

### Coupons:
- `WELCOME10` - ส่วนลด 10%
- `NEWYEAR2025` - ส่วนลด 15%
- `STEAM20` - ส่วนลด 20% สำหรับ Steam
- `FLAT50` - ส่วนลด 50 บาท
- `FIRSTBUY` - ส่วนลด 20% สำหรับผู้ซื้อครั้งแรก

## หมายเหตุ
- Migration script จะข้าม tables/indexes ที่มีอยู่แล้ว (ไม่ทับข้อมูลเดิม)
- Seed script จะข้ามข้อมูลที่ซ้ำ (ไม่สร้าง duplicate)
- ถ้ามี error ระหว่างรัน ให้ตรวจสอบ database connection ใน `.env`

