# 🗑️ วิธีลบตารางใน Supabase

## 🎯 วิธีที่ 1: ลบผ่าน SQL Editor (แนะนำ)

### ขั้นตอน:

1. **เปิด Supabase Dashboard**
   - ไปที่ [https://supabase.com](https://supabase.com)
   - Sign in และเลือก Project ของคุณ

2. **เปิด SQL Editor**
   - คลิกที่ **"SQL Editor"** ใน Sidebar ด้านซ้าย

3. **Copy และ Paste SQL**
   - เปิดไฟล์ `backend/drop_tables.sql` ในโปรเจคของคุณ
   - **Copy ทั้งหมด** (Ctrl+A แล้ว Ctrl+C)
   - **Paste** ใน SQL Editor (Ctrl+V)

4. **Run SQL**
   - กดปุ่ม **"Run"** (สีเขียว) หรือ **Ctrl+Enter**
   - รอให้รันเสร็จ

5. **ตรวจสอบผลลัพธ์**
   - จะเห็นข้อความ **"Success"** หรือ **"DROP TABLE"**
   - ตารางทั้งหมดจะถูกลบออก

---

## 🎯 วิธีที่ 2: ลบผ่าน Table Editor (ทีละตาราง)

### ขั้นตอน:

1. **เปิด Table Editor**
   - ไปที่ **"Table Editor"** ใน Sidebar ด้านซ้าย

2. **เลือกตารางที่ต้องการลบ**
   - คลิกที่ตารางที่ต้องการลบ (เช่น `games`)

3. **ลบตาราง**
   - คลิกที่ **"..."** (เมนู) ด้านบนขวาของตาราง
   - เลือก **"Delete table"** หรือ **"Drop table"**
   - ยืนยันการลบ

4. **ทำซ้ำกับตารางอื่นๆ**
   - ทำซ้ำกับตารางอื่นๆ ที่ต้องการลบ

---

## 🎯 วิธีที่ 3: ใช้ SQL Command โดยตรง

### Copy SQL นี้ไปรันใน SQL Editor:

```sql
-- ลบตารางทั้งหมด (เรียงตามลำดับ Foreign Key)
DROP TABLE IF EXISTS mystery_box_items CASCADE;
DROP TABLE IF EXISTS box_items CASCADE;
DROP TABLE IF EXISTS reviews CASCADE;
DROP TABLE IF EXISTS wishlists CASCADE;
DROP TABLE IF EXISTS transactions CASCADE;
DROP TABLE IF EXISTS game_codes CASCADE;
DROP TABLE IF EXISTS mystery_boxes CASCADE;
DROP TABLE IF EXISTS coupons CASCADE;
DROP TABLE IF EXISTS topup_history CASCADE;
DROP TABLE IF EXISTS games CASCADE;
DROP TABLE IF EXISTS users CASCADE;
DROP TABLE IF EXISTS categories CASCADE;
```

---

## ⚠️ ข้อควรระวัง

### 1. **ข้อมูลจะหายไปทั้งหมด**
- เมื่อลบตาราง ข้อมูลทั้งหมดในตารางจะหายไป
- **ไม่สามารถกู้คืนได้** (ยกเว้นมี Backup)

### 2. **Foreign Key Constraints**
- ใช้ `CASCADE` เพื่อลบ Foreign Key constraints อัตโนมัติ
- หรือลบตารางที่มี Foreign Key ก่อน

### 3. **ลำดับการลบ**
- ลบตารางที่มี Foreign Key ก่อน (child tables)
- แล้วค่อยลบตารางหลัก (parent tables)

---

## 📋 ลำดับการลบตาราง (ถูกต้อง)

1. `mystery_box_items` - มี Foreign Key ไปยัง `mystery_boxes`
2. `box_items` - มี Foreign Key ไปยัง `mystery_boxes`
3. `reviews` - มี Foreign Key ไปยัง `games` และ `users`
4. `wishlists` - มี Foreign Key ไปยัง `games` และ `users`
5. `transactions` - มี Foreign Key ไปยัง `game_codes`, `games`, `users`
6. `game_codes` - มี Foreign Key ไปยัง `games` และ `users`
7. `mystery_boxes` - ตารางหลัก
8. `coupons` - ตารางหลัก
9. `topup_history` - มี Foreign Key ไปยัง `users`
10. `games` - มี Foreign Key ไปยัง `categories`
11. `users` - ตารางหลัก
12. `categories` - ตารางหลัก

---

## ✅ หลังจากลบตารางแล้ว

1. **รัน database.sql ใหม่**
   - ไปที่ SQL Editor
   - Copy `backend/database.sql` → Paste → Run

2. **รัน seed.sql (ถ้าต้องการ)**
   - Copy `backend/seed.sql` → Paste → Run

3. **ตรวจสอบ Tables**
   - ไปที่ Table Editor
   - ตรวจสอบว่าตารางถูกสร้างใหม่แล้ว

---

## 🎯 สรุปขั้นตอนแบบเร็ว

1. **Supabase Dashboard** → **SQL Editor**
2. **Copy** `backend/drop_tables.sql` → **Paste** → **Run**
3. **Copy** `backend/database.sql` → **Paste** → **Run**
4. **Copy** `backend/seed.sql` → **Paste** → **Run** (ถ้าต้องการ)
5. **ตรวจสอบ** Tables ใน Table Editor

---

**⚠️ ระวัง: การลบตารางจะทำให้ข้อมูลหายไปทั้งหมด!**

